// A Settled line or catalogue row sharing a run of words with a page its own pointer does not name, which is the
// Argument restated rather than the direction the line exists to make greppable
export interface RestatedPointerFinding {
  line: string;
  otherPath: string;
  path: string;
}
