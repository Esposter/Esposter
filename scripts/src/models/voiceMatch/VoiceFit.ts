// One catalogue voice fitted to one character: the adjustments the card would carry, and the composite they score
export interface VoiceFit {
  pitch: number;
  rate: number;
  score: number;
  voice: string;
}
