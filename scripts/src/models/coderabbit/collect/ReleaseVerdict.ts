// The one line the verdict session writes, and the verb a recorded verdict is re-applied by
export enum ReleaseVerdict {
  // The release is a person's — the reason follows the verb on the same line
  Hold = "hold",
  Merge = "merge",
}
