// The machine-readable half of the walkthrough's merge risk block, in the bot's own spelling
export interface MergeRiskCoverage {
  coveredCommitId: string;
  kind: string;
  sourceCommitId: string;
}
