// The family alias a headless session runs on, never a version (`model-delegation`): left unpinned, a role runs
// On whatever the family's newest member is, which no log line would have to state. Every role runs on the same
// One today; the enum is what a role moved to another family — another vendor's, even — changes in one place,
// And `SessionRoleModelMap` is where a role says which it runs on.
export enum SessionModel {
  Opus = "opus",
}
