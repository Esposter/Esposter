export const NPM_REGISTRY_URL = "https://registry.npmjs.org";

// The workspace root every package sits under, per `pnpm-workspace.yaml`.
export const PACKAGES_DIRECTORY = "packages";

export const REGISTRY_FETCH_TIMEOUT_MS = 10000;
