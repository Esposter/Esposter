{
  "version": "2.0.0",
  "tasks": [
    {
      "type": "func",
      "label": "func: host start",
      "command": "host start",
      "problemMatcher": "$func-node-watch",
      "isBackground": true,
      "dependsOn": "bun --bun i (functions)",
      "options": {
        "cwd": "${workspaceFolder}/packages\\webhook"
      }
    },
    {
      "type": "shell",
      "label": "bun --bun i (functions)",
      "command": "bun --bun i",
      "options": {
        "cwd": "${workspaceFolder}/packages\\webhook"
      }
    }
  ]
}
