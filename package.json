{
  "name": "esposter",
  "version": "1.0.0",
  "description": "A nice and casual place for posting random things.",
  "keywords": [
    "azure",
    "chat",
    "drizzle-orm",
    "monorepo",
    "nuxt",
    "phaser",
    "pinia",
    "pnpm",
    "postgresql",
    "pwa",
    "realtime",
    "serverless",
    "social-platform",
    "trpc",
    "typescript",
    "unocss",
    "vite",
    "vue",
    "vuetify",
    "zod"
  ],
  "homepage": "https://github.com/Esposter/Esposter#readme",
  "bugs": {
    "url": "https://github.com/Esposter/Esposter/issues"
  },
  "license": "Apache-2.0",
  "author": "Jimmy Chen",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Esposter/Esposter.git"
  },
  "type": "module",
  "scripts": {
    "bench": "pnpm -r --workspace-concurrency=1 --if-present run bench",
    "bench:ci": "vitest bench --run",
    "build": "pnpm --filter \"@esposter/web^...\" --filter \"{.}^...\" run build && pnpm build:web",
    "build:docs": "virrun -- typedoc",
    "build:functions": "pnpm -C apps/functions run build",
    "build:infra": "pnpm -C apps/infra run build",
    "build:packages": "pnpm -r --filter \"./packages/*\" run build",
    "build:web": "virrun -- pnpm -C apps/web run build",
    "coverage": "vitest run --coverage",
    "crossOS": "pnpm -C scripts run crossOS",
    "format": "oxfmt",
    "format:check": "oxfmt --check",
    "graph:gen": "pnpm -C scripts run graph:gen",
    "lint": "virrun -- oxlint --format=default --disable-nested-config && virrun -- eslint . && virrun -- pnpm -r --parallel run lint",
    "lint:fix": "virrun -- oxlint --format=default --fix --disable-nested-config && virrun -- eslint --fix . && virrun -- pnpm -r --parallel run lint:fix",
    "lint:fix:packages": "virrun -- oxlint --format=default --fix --disable-nested-config packages && virrun -- pnpm -r --filter \"./packages/*\" --parallel run lint:fix",
    "lint:packages": "virrun -- oxlint --format=default --disable-nested-config packages && virrun -- pnpm -r --filter \"./packages/*\" --parallel run lint",
    "outdated:dependencies": "pnpm -C scripts run outdated:dependencies",
    "prepare": "git config core.hooksPath .githooks || true",
    "pwsh": "pwsh -ExecutionPolicy Unrestricted",
    "refresh:lockfile": "pnpm crossOS refresh:lockfile",
    "release": "pnpm format:check && pnpm build:packages && pnpm lint:packages && pnpm typecheck:packages && pnpm test:packages && lerna publish --yes",
    "start": "pnpm -C apps/web run start",
    "sweep:constant-scope": "pnpm -C scripts run sweep:constant-scope",
    "sweep:repeated-list-items": "pnpm -C scripts run sweep:repeated-list-items",
    "sweep:shared-export-consumers": "pnpm -C scripts run sweep:shared-export-consumers",
    "sweep:skill-docs": "pnpm -C scripts run sweep:skill-docs",
    "sweep:unterminated-results": "pnpm -C scripts run sweep:unterminated-results",
    "test": "virrun -- vitest run",
    "test:packages": "virrun -- vitest run --project \"packages/*\"",
    "typecheck": "virrun -- tsc && virrun -- pnpm -r --parallel run typecheck",
    "typecheck:packages": "virrun -- pnpm -r --filter \"./packages/*\" --parallel run typecheck",
    "update:node": "pnpm -C scripts run update:node",
    "watch:packages": "pnpm -r --filter \"./packages/*\" --parallel exec tsdown --watch"
  },
  "devDependencies": {
    "@esposter/configuration": "workspace:*",
    "@esposter/shared-node": "workspace:*",
    "@lerna-lite/cli": "catalog:",
    "@lerna-lite/publish": "catalog:",
    "@types/node": "catalog:",
    "@vitest/coverage-v8": "catalog:",
    "dotenv": "catalog:",
    "eslint": "catalog:",
    "oxfmt": "catalog:",
    "oxlint": "catalog:",
    "oxlint-tsgolint": "catalog:",
    "typedoc": "catalog:",
    "typescript": "catalog:",
    "virrun": "workspace:*",
    "vitest": "catalog:"
  },
  "devEngines": {
    "runtime": {
      "name": "node",
      "version": "^26.8.2",
      "onFail": "warn"
    }
  },
  "engines": {
    "node": "^26.8.2"
  },
  "packageManager": "pnpm@12.3.4+sha512.961aa41fb077da3a04a441d9f8e15ebc0c96da8ef710b2eb67bf9ee7cb0610eabd48f1fd85f51cffe73846785fa0f87c56a3a872a1d893f8446741b5cce45457",
  "crossOS": {
    "refresh:lockfile": {
      "darwin": "sh ./scripts/src/refreshLockfile/index.sh",
      "linux": "./scripts/src/refreshLockfile/index.sh",
      "win32": "pnpm pwsh scripts/src/refreshLockfile/index.ps1"
    },
    "update:node": {
      "darwin": "sh ./scripts/src/updateNode/install.sh",
      "linux": "./scripts/src/updateNode/install.sh",
      "win32": "pnpm pwsh scripts/src/updateNode/install.ps1"
    }
  },
  "scriptsComments": {
    "bench:ci": "Not a shorter `pnpm bench`, and never run in its place. `bench` is `pnpm -r --workspace-concurrency=1`: every member owning a bench, one at a time, rewriting the committed `*.bench.md` a developer diffs. The concurrency flag is load-bearing twice over, because parallel benches skew each other's numbers and because the `scripts` cold-build bench deletes each `packages/*/dist` out from under a concurrent member still loading its `vitest.config.ts`, which surfaces as `Failed to resolve entry for package`. This one is a bare `vitest bench --run` at the root: one process over every project's bench files at once, which measures nothing anybody should compare and proves only that they all still execute. That is what the Bench workflow wants, and it is a script so the workflow names one rather than restating the command. See the `bench` skill.",
    "build": "A deviation taken on purpose: the bare name means the whole workspace in a pnpm, turbo or nx repo alike, and here it means the web app plus the libraries it imports, because Railway runs `pnpm build` as its default build command. That makes this name a deploy entrypoint rather than a developer convenience, and widening it would have every deploy build two bundles it never ships. Two selectors, both derived rather than named. `--filter \"@esposter/web^...\"` is everything the app imports, in topological order, so nothing here knows which libraries exist. `--filter \"{.}^...\"` is what the root itself depends on, which is where the toolchain is declared — `virrun` above all, whose committed `bin/cli.js` imports a `dist` only its own build writes. The app does not import it, so the first selector cannot reach it, and a deploy that builds from source rather than from CI's `package-builds` artifact then dies inside `build:web` on `Cannot find module '.../packages/virrun/dist/cli.js'`. That is the whole reason for the second selector: here the toolchain is a build input, not a dependency. `^...` rather than the `{.}...` that `setup-packages` installs, because this one is a `run` — matching the root would have `build` recurse into itself. The two halves stay separate commands because only the second one belongs in the sandbox — a library's output is platform-neutral and read from host disk, so `virrun` would charge it the mirror tax for an isolation nothing wants, while the app build is the one that needs the Linux-generated `.nuxt` (see the virrun adoption page). `build:packages` is a third set: every library rather than the ones this app imports, which is what CI caches and hands to the checks, and the coverage shards build the two apps whose bundles the suite asserts against. @TODO: restore `pnpm build:docs` to the chain when a bridge build grows the AST navigation API. TypeDoc exits 6 against the `typescript-native-bridge` alias, and `build` is what deploy runs, so the deploy would fail on documentation alone. `build:docs` stays as the probe. See the `build-docs` job in .github/workflows/CI.yaml."
  }
}
