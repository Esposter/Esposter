# GitHub Actions

Workflow and composite-action authoring — the runner's own affordances, template data through `env:`, skipped jobs satisfying their checks, and schedules.

| Unit                                                                             | Swept | Notes |
| -------------------------------------------------------------------------------- | ----- | ----- |
| `.github/workflows/CI.yaml`                                                      | —     |       |
| `.github/workflows` — `Release`, `build-packages`, `deploy-function-app`         | —     |       |
| `.github/workflows` — `ReviewCollector`, `run-review-collector`, `claude-warmup` | —     |       |
| `.github/workflows` — `Bench`, `CodeQL`, `DeleteMergedBranch`, `Pulumi`          | —     |       |
| `.github/actions`                                                                | —     |       |
