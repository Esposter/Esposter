# Dependency Updates

The catalog, the Renovate rules and the pins — carets, holds as `packageRules`, overrides, action SHAs and the one node pin.

| Unit                                             | Swept | Notes |
| ------------------------------------------------ | ----- | ----- |
| `pnpm-workspace.yaml` — the catalog              | —     |       |
| `pnpm-workspace.yaml` — settings and `overrides` | —     |       |
| `renovate.json`                                  | —     |       |
| `.github` — action pins                          | —     |       |
| `.node-version` and every manifest's node fields | —     |       |
