# Tooling

`scripts/`, `.agents/`, the app's root configuration and `content/`.

| Unit                                       | Swept      | Notes                                                                                                                  |
| ------------------------------------------ | ---------- | ---------------------------------------------------------------------------------------------------------------------- |
| `scripts/src/oxlint`, `scripts/src/sweeps` | 2026-09-06 | `args` has no long spelling a strict-mode binding may take, so it is the settled one; an ESTree field name is estree's |
| `scripts` — the rest                       | 2026-09-15 | `outdatedDependencies`, `updateNode`, `dependencyGraph`, `services`, `models`, `workspace`, the root files             |
| `apps/web/content`                         | 2026-09-15 | the docs suites beside the pages — the pages themselves are the `docs` ledger's                                        |
