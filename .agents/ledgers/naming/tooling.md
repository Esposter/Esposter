# Tooling

`scripts/`, `.agents/`, the app's root configuration and `content/`.

| Unit                                              | Swept                 | Notes                                                                                                                  |
| ------------------------------------------------- | --------------------- | ---------------------------------------------------------------------------------------------------------------------- |
| `scripts/src/oxlint`, `scripts/src/sweeps`        | 2026-09-25 · Opus 5.5 | `args` has no long spelling a strict-mode binding may take, so it is the settled one; an ESTree field name is estree's |
| `scripts` — the rest                              | 2026-09-25 · Opus 5.5 | `outdatedDependencies`, `updateNode`, `dependencyGraph`, `services`, `models`, `workspace`, the root files             |
| `apps/web` — the root configuration and `scripts` | 2026-09-25 · Opus 5.5 | `nuxt.config`, `uno.config`, `configuration/`, `scripts/tiled`, `scripts/phaser`                                       |
| `apps/web/content`                                | 2026-09-25 · Opus 5.5 | the docs suites beside the pages — the pages themselves are the `docs` ledger's                                        |
