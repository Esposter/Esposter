# Messaging

| Unit                                                                                      | Swept      | Notes                                                                                                                                                                                                                                                                                                                                                                                     |
| ----------------------------------------------------------------------------------------- | ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `store/message`                                                                           | 2026-08-20 | The reaction toggle both the chip and the quick-reaction bar implement is one `toggleEmoji`; `sentMessage` dropped an `offset` ref its own item count already was                                                                                                                                                                                                                         |
| `Message/Model/Message`                                                                   | 2026-08-10 | `getSuggestionListTitle`, `getFileCornerStyle`, `useFocusWhenActive`, `setCurrentRoomId` test helper                                                                                                                                                                                                                                                                                      |
| `Message/Model/Room`                                                                      | 2026-08-10 | `Styled/SkeletonListItem`, `Room/BaseListItem`, `Settings/Field`, `DetailPlaceholder`; two behaviour fixes                                                                                                                                                                                                                                                                                |
| `Message/Model/User`                                                                      | 2026-08-10 | Profile card resolved through `getDisplayName`; three device-select wrappers deleted; activator stacks onto the Styled primitives                                                                                                                                                                                                                                                         |
| `Message/Model`                                                                           | 2026-08-11 | `Settings/Header.vue` shared, Room adopting the shell's `#header` slot; the PDF viewer's hand-rolled dialog composes `StyledDialog`; every file renderer declared in `TypeRendererMap`; `is*` carried onto four dialog flags                                                                                                                                                              |
| `Message/Content`                                                                         | 2026-08-20 | The six controls both call bars carry became `Control/Group`; the device menu's open-and-refresh moved into `useCallDeviceSettings`; the knocker's two actions onto one bound helper; the image field onto `useUploadImage`; why no tooltip wrapper exists for a text button written into `vuetify`; the call view's two screen-share stops settled as a `vue-page-composition` carve-out |
| `Message/DraftsAndSent`, `Message/RightSideBar`, `Message/LeftSideBar`, `Message/Friends` | 2026-08-20 | `Search/AddIcon` takes `v-hover`'s own prop rather than five callers coercing its null; the five friends row actions share one prop shape; a draft row keyed by its room rendered a room and thread draft under one key                                                                                                                                                                   |
| `app/composables/message`                                                                 | 2026-08-20 | The two drafts-and-sent readers each hand-rolled the appending page read that offset pagination now owns, and the parity gap with the cursor version is named in that commit                                                                                                                                                                                                              |
| `app/services/message`                                                                    | 2026-08-20 | `EmojiItem` was a second name for `Emoji`; the `:slug:` wrap, the filter keyword and the invalid-value error each own one home now, and three filter types shared one async loader                                                                                                                                                                                                        |

`Message/Model` is two-thirds of `app/components/Message`, so the units split at its sub-directories.
