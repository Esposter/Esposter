# Products

The app's smaller products — everything under `app/components` that is neither messaging nor the resource explorer. Each row carries a product's components together with the store, composable, service and page files only it uses.

| Unit                                                                                                                  | Swept      | Notes                                                                                                                                                                                                         |
| --------------------------------------------------------------------------------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `Post` + `store/post`, `composables/post`, `services/post`, `pages/post`                                              | 2026-08-20 | `useReadPosts` absorbed its user-filtered twin; `PostVoteButton` owns the vote cascade both directions had written out, pinned by a test; three re-emit shims inlined                                         |
| `Clicker` + `store/clicker`, `composables/clicker`, `services/clicker`, `pages/clicker.vue`                           | 2026-08-20 | `shared/models/clicker` swept 2026-08-12 — shared models not in scope; the two timer composables became `useWorkerInterval`, pinned by an unmount test; icon globs hoisted to module-scope maps               |
| `User`, `Achievement` + `store/achievement`, the `user`/`achievement`/`room` layers, `pages/user`, `achievements.vue` | 2026-08-20 | `useReadUserAchievements` absorbed the snackbar list's copy of the read; one `getUnlockedUserAchievements` behind three filters; `CategoryColorMap` replaced the switch; dead `isAchievementUnlocked` deleted |
| `Docs` + `composables/docs`, `services/docs`, `pages/docs`, `pages/[...slug].vue`                                     | 2026-08-20 | thin pass — one `getSlug` behind four copies of the last-path-segment idiom, and the toc listeners stopped taking bare function references                                                                    |
| `RichTextEditor` + `composables/codemirror`, `services/codemirror`                                                    | 2026-08-20 | one `FooterBarSlotProps` for both slots; the audio recorder's tooltip chain became `StyledTooltipIconButton` and its raw 1000 a dayjs duration                                                                |
| `Visual`, `Anime`, `About`, `Login`, `Transition` + their composable/service layers and the pages that mount them     | 2026-08-20 | the supporter cards and accent classes moved to `services/about`; the globe's three copies of its random-colour pick became one, with the off-by-one it hides raised rather than fixed                        |

The 2026-08-20 pass resumed from the files changed since each row's previous date. One finding: the achievement
grid item wrote the same two fallbacks three times across its progress bar and the count beneath it. The post
byline's link-or-not branching stays as it is — collapsing it through `component :is` would put a wrapper element
around the avatar that the unlinked branch does not have, which is a layout change rather than a simplification.
