# String Utils

`pluralize` over a count ternary, `normalizeString` as the default trim, sanitizers at the Zod boundary, and token matching in authored content.

| Unit                                                        | Swept | Notes |
| ----------------------------------------------------------- | ----- | ----- |
| `packages/shared` — the helpers themselves                  | —     |       |
| `apps/web/shared`, `packages/db-schema` — the Zod boundary  | —     |       |
| `apps/web/app/components`                                   | —     |       |
| `apps/web/app` — `composables`, `services`, `store`, `util` | —     |       |
| `apps/web/server`                                           | —     |       |
| `apps/functions`, the remaining `packages`                  | —     |       |
