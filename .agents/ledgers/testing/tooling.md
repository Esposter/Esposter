# Tooling

The repo's own scripts and the app's config suites — what tests the toolchain rather than a product.

| Unit                                             | Swept      | Notes                                                           |
| ------------------------------------------------ | ---------- | --------------------------------------------------------------- |
| `scripts`                                        | 2026-08-30 |                                                                 |
| `apps/web/uno.config`, `apps/web/vuetify.config` | 2026-08-30 | both resolved-config snapshots stay — see the README            |
| `apps/web/content`                               | 2026-08-20 | `docs/index.test.ts` — top-level await fixtures, see the README |
