# Infrastructure v1 Roadmap

Safe Pulumi-only cleanup. No application migration or manual Azure preparation required.

## Scope

- Keep all imported resource names.
- Keep `protect: true`.
- Avoid auth, network, or SKU changes that can break current app behavior.
- Apply only changes that are safe to preview and roll forward independently.

## Work Items

- [x] Review public Web PubSub Trace access in development.
- [x] Review public Web PubSub Trace access in production.
- [x] Correct Speech Services `Application` tag from `Eslife` to `Esposter`.
- [x] Run `pnpm infra:preview`.
- [x] Confirm preview only includes the Web PubSub ACL and Speech tag changes.
- [x] Run `pnpm infra:up`.
- [x] If apply partially succeeds or fails, rerun `pnpm infra:preview` before any follow-up apply.
- [x] Move this file to `features/infra/completed/v1.md`.

## Apply Notes

- 2026-05-17: `pnpm infra:preview` showed exactly three updates: two Web PubSub ACL updates and one Speech tag update.
- 2026-05-17: `pulumi up --yes` partially applied the Speech tag update and failed on the Web PubSub ACL updates.
- 2026-05-17: Targeted Web PubSub apply showed Azure rejects ACL changes on `Free_F1` with `Current SKU Free_F1 does not support changing network ACLs`.
- 2026-05-17: Web PubSub Trace access remains intentionally unchanged while the service stays on `Free_F1`.
- 2026-05-17: Reran `pnpm infra:preview` after each failed apply before any follow-up action.

## Expected Pulumi Changes

- `Microsoft.SignalRService/webPubSub`:
  - no v1 apply; `Free_F1` does not support changing network ACLs.
- `Microsoft.CognitiveServices/accounts`:
  - update `tags.Application` to `Esposter`.
