# Infrastructure v9 Roadmap

Development stateful naming wave.

## Re-do: v6–v8 Stateless Resources

**Complete.** All dev stateless resources have been re-done with the correct `ae` token. Final Azure names and Pulumi parents are now aligned for:

| Resource Type               | Final Azure Name                 | Pulumi Parent                                                            |
| --------------------------- | -------------------------------- | ------------------------------------------------------------------------ |
| Action groups 001–003       | `dev-ag-esposter-001..003`       | `devRgEsposterAe001`                                                     |
| Logic Apps 001–004          | `dev-logic-esposter-ae-001..004` | `devRgEsposterAe001`                                                     |
| API connections 001–004     | `dev-apic-esposter-ae-001..004`  | `devRgEsposterAe001`                                                     |
| Event subscriptions 001–002 | `dev-evgs-esposter-ae-001..002`  | (deferred — pending `devEvgtEsposterAe001`)                              |
| Budgets 001–002             | `dev-bgt-esposter-001..002`      | (no parent; scope still points to legacy RG until function app migrates) |

## Scope

Move development stateful services and externally referenced endpoints to the official naming convention. This wave should create target resources with the correct `parent` option from the start wherever the parent resource is already at a final name.

## Candidate Resources

| Legacy Resource                  | Target Resource            | Cutover Inputs                                                                                                  |
| -------------------------------- | -------------------------- | --------------------------------------------------------------------------------------------------------------- |
| `d-shp-rg-esposter-auea-001`     | `dev-rg-esposter-ae-001`   | **Already created.** Parent for most target development resources.                                              |
| `dshpstespauea001`               | `devstesposter001`         | Storage data copy, blob URLs, connection strings, role assignments, blob service properties, management policy. |
| `dshpsrchespauea001`             | `dev-srch-esposter-001`    | Index/schema recreation and `AZURE_SEARCH_BASE_URL` / `AZURE_SEARCH_API_KEY`.                                   |
| `d-shp-spch-esposter-auea-001`   | `dev-spch-esposter-001`    | Speech key/endpoint consumers if any are active.                                                                |
| `d-shp-func-esposter-auea-001`   | `dev-func-esposter-001`    | Function app settings, host keys, managed identity role assignments, App Service plan.                          |
| `d-shp-pubsub-esposter-auea-001` | `dev-wps-esposter-001`     | Web PubSub connection string.                                                                                   |
| `d-shp-evgt-esposter-auea-001`   | `dev-evgt-esposter-ae-001` | Event Grid endpoint/key and function publisher role assignment.                                                 |
| `d-shp-log-esposter-auea-001`    | `dev-log-esposter-ae-001`  | **Created in v10.** App Insights workspace link and Logic Apps Management solution.                             |
| `d-shp-appi-esposter-auea-001`   | `dev-appi-esposter-ae-001` | **Created in v10.** Function App app settings cutover and Smart Detector rules cutover.                         |

## Work Items

- [x] Complete re-do of v6–v8 stateless resources (see Re-do section above).
- [x] v10 monitoring complete — `dev-log-esposter-ae-001` and `dev-appi-esposter-ae-001` live; legacy monitoring stack removed; `d-shp-func-esposter-auea-001` app settings already cut to new App Insights.
- [x] Pulumi code written for all target dev resources (storage, search, ASP, func, WPS, evgt, deployment principal role assignments). Build, typecheck, and lint clean.
- [ ] Deploy: run `pnpm infra:up` to create `devstesposter001`, `dev-srch-esposter-001`, `dev-asp-esposter-ae-001`, `dev-func-esposter-001`, `dev-wps-esposter-001`, `dev-evgt-esposter-ae-001` in Azure.
- [ ] **Data migration (cutover prerequisite):** copy all blob containers, table entities, and queues from `dshpstespauea001` to `devstesposter001`. Document any containers/tables/queues that can be rebuilt from scratch rather than copied.
- [ ] Recreate search indexes on `dev-srch-esposter-001` (schemas only; no data to copy).
- [ ] Get managed identity principal IDs for `devFuncEsposter001` and `devSrchEsposter001` from Azure after deploy. Add named constants `DevFuncEsposter001PrincipalId` and `DevSrchEsposter001PrincipalId`. Create role assignment files for both.
- [ ] Configure `dev-func-esposter-001` app settings: `dev-appi-esposter-ae-001` connection string, `devstesposter001` connection string, `dev-wps-esposter-001` connection string.
- [ ] Update `dev-evgs-esposter-ae-001..002` scope to `devEvgtEsposterAe001` and destination to `devFuncEsposter001` after Event Grid topic migration.
- [ ] Update Website Contributor role assignment scopes (logic-001, 002, 004) to `devFuncEsposter001` after Function App migration.
- [ ] Update EventGrid Contributor role assignment scopes (logic-003, 004) to `devEvgtEsposterAe001` after Event Grid topic migration.
- [ ] Update development app and function environment values (storage connection string, search URL/key, Web PubSub connection string, Event Grid endpoint/key, function host key).
- [ ] Run development smoke tests for uploads, messages, push notifications, search, Web PubSub, and function processing.
- [ ] Update budget scopes and filters to `dev-rg-esposter-ae-001` after all filtered resources are migrated.
- [ ] Remove legacy development stateful resources only after smoke tests pass.

## Current Status

Pulumi code written for all target dev resources. Build clean. Next: run `pnpm infra:up` to deploy, then handle data migration and cutover in sequence (storage data copy → search index recreation → func app settings → env var cutover → smoke tests → legacy removal).

## Pulumi Notes

Set `parent` on each legacy-parentless resource in the same wave that migrates its parent, using the create/cutover/delete pattern:

| Resource                                                   | `parent` to set        | When                       |
| ---------------------------------------------------------- | ---------------------- | -------------------------- |
| `dev-logic-esposter-ae-001..004`                           | `devRgEsposterAe001`   | **Done** — already set     |
| `dev-ag-esposter-001..003`                                 | `devRgEsposterAe001`   | **Done** — already set     |
| `dev-evgs-esposter-ae-001..002`                            | `devEvgtEsposterAe001` | Event Grid topic migration |
| Website Contributor role assignments (logic-001, 002, 004) | `devFuncEsposterAe001` | Function App migration     |
| EventGrid Contributor role assignments (logic-003, 004)    | `devEvgtEsposterAe001` | Event Grid topic migration |

**Principal ID pattern** (`applyPrincipalId` is removed): After creating the target Function App and Search service, get their managed identity principal IDs from Azure and add named constants (`DevFuncEsposterAe001PrincipalId`, `DevSrchEsposter001PrincipalId`) since each identity is used across multiple role assignment files. Use inline literals only for single-use role assignments.

**Budget scope**: `dev-bgt-esposter-001` and `dev-bgt-esposter-002` scope strings currently point to `d-shp-rg-esposter-auea-001`. Update to `dev-rg-esposter-ae-001` in the same apply that removes the last filtered legacy resource.

## Rollback

- Restore development app and function environment values to legacy endpoints and keys.
- Disable target Event Grid subscriptions if they are delivering duplicate events.
- Keep legacy storage, search, function, Web PubSub, Event Grid, and monitoring resources intact until the rollback window closes.
