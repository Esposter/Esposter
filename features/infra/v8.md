# Infrastructure v8 Roadmap

Finalized low-risk naming wave.

## Scope

Redo the naming migration playbook from the beginning using the finalized naming rules in `packages/infra/docs/naming-conventions.md`.

This wave handles low-risk, replayable, or briefly parallel resources before moving into stateful service endpoint cutovers. It starts from the current clean baseline where the previous v8/v10 target resources are live and Pulumi-owned. Those resources are now treated as the legacy source for the finalized wave when their names still include outdated tokens.

## Baseline

- [x] Run `pnpm --filter @esposter/infra infra:preview --suppress-outputs`.
- [x] Confirm baseline preview is clean: `104 unchanged`.
- [x] Run live inventory for the finalized v8 candidate set.
- [x] Compare Pulumi state with live Azure for candidate resources.

## Finalized Naming Rules Used By This Wave

- Regional Azure resources keep the region token: `<environment>-<assetType>-<workload>-<region>-<index>`.
- Globally named Azure resources that allow hyphens omit the region token: `<environment>-<assetType>-<workload>-<index>`.
- Global compact resources that do not allow hyphens use: `<environment><assetType><workload><index>`.
- Legacy `shp`, single-letter environment tokens, `bdg`, `apicn`, `evgts`, and `pubsub` are not valid target tokens.
- Add Pulumi `parent` only when the actual parent resource is already target-named or is being migrated in the same wave.

## Candidate Resources

| Current Resource                    | Finalized Target Resource         | Notes                                                                                 |
| ----------------------------------- | --------------------------------- | ------------------------------------------------------------------------------------- |
| `dev-ag-esposter-auea-001..003`     | `dev-ag-esposter-001..003`        | Global resource; create target action groups first, then update budget/alert refs.    |
| `prod-ag-esposter-auea-001..003`    | `prod-ag-esposter-001..003`       | Same as dev; refresh Logic App callback URLs only if receiver workflow IDs change.    |
| `d-shp-bdg-esposter-auea-001..002`  | `dev-bgt-esposter-001..002`       | Global-ish resource-group budget name; use finalized `bgt` token and no region token. |
| `p-shp-bdg-esposter-auea-001..002`  | `prod-bgt-esposter-001..002`      | Same as dev; keep scope and thresholds identical.                                     |
| `dev-apic-esposter-auea-001..004`   | `dev-apic-esposter-ae-001..004`   | Regional resource; replace after target action groups are live.                       |
| `prod-apic-esposter-auea-001..004`  | `prod-apic-esposter-ae-001..004`  | Regional resource; same pattern as dev.                                               |
| `dev-logic-esposter-auea-001..004`  | `dev-logic-esposter-ae-001..004`  | Regional resource; recreate after target API connections exist. Parent deferred.      |
| `prod-logic-esposter-auea-001..004` | `prod-logic-esposter-ae-001..004` | Regional resource; same pattern as dev. Parent deferred.                              |
| `dev-evgs-esposter-auea-001..002`   | `dev-evgs-esposter-ae-001..002`   | Scoped to regional topic; recreate after target Logic Apps/roles are live.            |
| `prod-evgs-esposter-auea-001..002`  | `prod-evgs-esposter-ae-001..002`  | Scoped to regional topic; same pattern as dev.                                        |

## Parent Migration Rules

Most existing low-risk resources must stay parentless in Pulumi until their real parent resources are migrated. Do not add a legacy parent just to clean up the tree.

| Resource                                                 | Final `parent`          | Apply Parent When                  |
| -------------------------------------------------------- | ----------------------- | ---------------------------------- |
| `dev-logic-esposter-ae-001..004`                         | `devRgEsposterAe001`    | Development legacy RG decommission |
| `dev-ag-esposter-001..003`                               | `devRgEsposterAe001`    | Development legacy RG decommission |
| `prod-logic-esposter-ae-001..004`                        | `prodRgEsposterAe001`   | Production legacy RG decommission  |
| `prod-ag-esposter-001..003`                              | `prodRgEsposterAe001`   | Production legacy RG decommission  |
| `dev-evgs-esposter-ae-001..002`                          | `devEvgtEsposterAe001`  | Development Event Grid migration   |
| `prod-evgs-esposter-ae-001..002`                         | `prodEvgtEsposterAe001` | Production Event Grid migration    |
| Website Contributor role assignments for Logic Apps      | Target Function App     | Function App migration             |
| EventGrid EventSubscription Contributor role assignments | Target Event Grid topic | Event Grid topic migration         |

## Work Items

- [x] Add finalized target action group files.
- [ ] Update Smart Detector rules and budgets to reference finalized action groups.
- [ ] Add finalized target budget files with identical scope, thresholds, filters, and notifications.
- [x] Run preview and confirm action-group create scope.
- [x] Apply finalized action-group create step.
- [x] Verify finalized action groups exist in Azure.
- [x] Run post-apply preview before any further applies.
- [x] Run preview and confirm budget/reference cutover scope.
- [x] Apply budget/reference cutover step.
- [x] Verify finalized action group receivers and budget notifications.
- [x] Run post-budget-cutover preview before cleanup.
- [x] Unprotect old action group and budget state only for the cleanup step.
- [x] Remove old action group and budget source files in a cleanup step after verification.
- [x] Run final clean preview.

## Current Status

Finalized target action groups are live in Azure and protected in Pulumi source. The action-group create preview showed exactly six creates and `104 unchanged`; the apply created all six action groups successfully.

Post-action-group preview is clean at `110 unchanged`.

Budget/reference cutover preview was clean and scoped: 4 finalized budget creates, 22 action group reference updates, 88 unchanged, and no deletes or replacements. The automated apply did not reach Azure changes because Pulumi hit a local gRPC timeout during its built-in preview (`14 UNAVAILABLE: No connection established`).

Direct `pnpm infra:up --yes --suppress-outputs` from `packages/infra` got past the gRPC issue, but Azure rejected the new budgets because monthly budget start dates cannot be earlier than the current month. The new `bgt` budget start dates were moved to `2026-05-01T00:00:00Z`; legacy budget start dates stay unchanged until cleanup.

The retried direct apply created all four finalized budgets. Azure inventory confirms `dev-bgt-esposter-001`, `dev-bgt-esposter-002`, `prod-bgt-esposter-001`, and `prod-bgt-esposter-002` exist with monthly grain, `0.01` amount, `2026-05-01T00:00:00Z` start, and `2035-12-31T00:00:00Z` end.

Post-budget-cutover preview is clean at `114 unchanged`. Cleanup can proceed.

Cleanup complete. Old `*-ag-esposter-auea-*` action groups and legacy `*-bdg-esposter-auea-*` budgets were deleted after a scoped cleanup preview. Final preview is clean at `104 unchanged`.

## Correction: Top-Down Parent Wave

The next migration should proceed top-down so final Azure names and final Pulumi parents land together. Do not continue creating finalized children inside legacy resource groups because those children would need another migration when `dev-rg-esposter-ae-001` and `prod-rg-esposter-ae-001` become their real parents.

Corrected order:

- [x] Remove accidental `dev/prod-apic-esposter-ae-001..004` API connections created in legacy resource groups. They are not referenced by Logic Apps yet.
- [x] Create final resource groups: `dev-rg-esposter-ae-001` and `prod-rg-esposter-ae-001`.
- [x] Recreate action groups in the final resource groups with final names and `parent` set to the final resource group.
- [ ] Recreate budgets in the final resource groups with final names, final scope, final resource filters, and `parent` set to the final resource group after the filtered Function App/Event Grid resources are also on final names.
- [x] Recreate API connections in the final resource groups with final names and `parent` set to the final resource group.
- [ ] Recreate Logic Apps in the final resource groups with final names and `parent` set to the final resource group.
- [ ] Recreate role assignments for new Logic App managed identities, parenting them to final Function App or Event Grid topic resources once those parents exist.
- [x] Cut action-group references to final resource-group action groups.
- [x] Clean up old action groups only after scoped previews and verification.
- [ ] Cut remaining references to final resources as their parent waves complete.
- [ ] Clean up old resources only after scoped previews and verification.

Accidental target-named API connections in the legacy resource groups were removed after unprotecting only those eight URNs. Final resource groups were then created as the top-level parents. Post-RG preview is clean at `106 unchanged`.

Target action groups were then created under `dev-rg-esposter-ae-001` and `prod-rg-esposter-ae-001` with final Azure names and final resource-group parents. The create preview showed exactly six creates and `106 unchanged`; apply created all six, and post-action-group preview is clean at `112 unchanged`.

Budget notifications and Smart Detector action-group IDs were cut over to the final-resource-group action groups in a references-only update: `22 updated`, `90 unchanged`. After a clean preview, the six old legacy-resource-group action groups were unprotected, source was removed, and cleanup applied as exactly six deletes. Final post-cleanup preview is clean at `106 unchanged`.

Final API connections were created under `dev-rg-esposter-ae-001` and `prod-rg-esposter-ae-001` with final Azure names and final resource-group parents. The create preview showed exactly eight creates and `106 unchanged`; apply created all eight. Live Azure API connection properties were then updated with full `PUT` calls to set `parameterValueType: "Alternative"` and `alternativeParameterValues: {}` because the Azure Native schema does not model that field. All eight final API connections report `Ready`, and post-fix preview is clean at `114 unchanged`.

## Pulumi Notes

- Budgets should move to final resource-group scope strings (`subscriptions/.../resourceGroups/dev-rg-esposter-ae-001` and `subscriptions/.../resourceGroups/prod-rg-esposter-ae-001`) only when their filtered resources also have final names. The RG-wide budgets can move with the resource group, but the filtered Function App/Event Grid budgets should not point a final RG budget at a legacy resource ID.
- Target action groups should keep the same receiver payloads as the current live action groups.
- API connections require live `parameterValueType: "Alternative"` verification after creation because the Azure Native schema does not model it. Use a full `PUT` with the existing location, tags, `properties.api`, `properties.displayName`, `parameterValueType: "Alternative"`, and `alternativeParameterValues: {}`; `PATCH` is rejected by Azure for API connection properties.
- Keep callback URL values in Pulumi config secrets. Do not commit callback URLs or generated Logic App export payloads.
- Do not delete old budget/action group resources in the same apply that creates the target resources and swaps references.

## Rollback

- Point budgets and Smart Detector rules back to the previous action groups.
- Keep old action groups and budgets protected until finalized resources are verified.
- If target budget creation fails, leave old budgets in place and remove the target files before retrying.
