# Infrastructure v6 Roadmap

Stack alignment and naming refactor.

## Scope

- Decide whether one Pulumi stack owns all resources or stacks are split by environment.
- Defer Azure renames unless replacement risk is explicitly accepted.

## Work Items

- [ ] Decide one stack vs separate environment stacks.
- [ ] If one stack, rename the imported stack to `prod` or `main`.
- [ ] If separate stacks, gate resource creation by environment and import resources into matching stacks.
- [ ] Decide whether current shorthand naming remains.
- [ ] Plan any resource renames as explicit replacement/migration work.
- [ ] Move this file to `features/infra/completed/v6.md`.

## Manual Inputs Needed

- Preferred final stack name.
- Whether development and production should have independent lifecycle management.
- Whether any Azure resource names are worth replacing for readability.
