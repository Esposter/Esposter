# Markdown Export

Expose the existing `serializeToMarkdown.ts` as an official export format.

## Why not

- Markdown is not a standard data-interchange format.
- Copy-to-clipboard already covers the shareability use case.
