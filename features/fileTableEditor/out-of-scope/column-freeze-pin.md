# Column Freeze / Pin

Freeze/pin arbitrary columns so they stay visible while scrolling.

## Why not

- Vuetify's data table does not actually support freezing arbitrary columns.
- The only workaround (reordering headers) is not worth the complexity.
