# Multi-Sheet XLSX

Import and merge multiple sheets from one XLSX workbook.

## Why not

- Single-sheet import is sufficient; `sheetIndex` is already configurable on `XlsxDataSourceItem`.
- Multi-sheet merging adds complexity with low value.
