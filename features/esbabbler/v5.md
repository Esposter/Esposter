# Esbabbler — v5 Roadmap

All features are free-to-implement (no paid infra) unless explicitly noted.
Built on existing Azure Table Storage, Postgres, Azure Blob, and Azure Function infrastructure.
Strikethroughs are acknowledged but deferred with rationale.

---

## Comfort gap analysis

After v4 the platform has: messaging, reactions, threads, drafts, voice, DMs, friends, full RBAC, full moderation (timeout/ban/warn/softban/word filter), slash commands, slowmode, read-only channels, system messages, link previews, file uploads, presence, user profiles, global search, push notifications, inbound webhooks, categories, pinned messages.

Core remaining friction points vs Discord: no role mentions, no room identity (icon), no per-room nickname, no group DM management. Resolved: unread indicators. Dropped: message permalinks (not needed).

---

## Quick Wins

1. **Collapsible embeds** — client-side localStorage, zero server cost
2. **Room avatar / icon** — Azure Blob SAS reusing existing upload flow
3. **Per-room nickname** — one nullable column on `usersToRooms`

---

## Composer & Message Display

- [ ] **Collapsible embeds** — per-message toggle to hide/show link previews and image attachments; state in `localStorage` keyed by message `rowKey`; small chevron on the embed container; no server involvement; mirrors Discord's "hide embed" eye button
- [x] ~~**Message permalink / deep link**~~ — already handled by existing room routing; deep link + scroll-to highlight deemed unnecessary

---

## Room Identity

- [ ] **Room avatar / icon** — `image: text("image").notNull().default("")` on `rooms` (column exists but is currently nullable — migration needed to add NOT NULL + DEFAULT ''); upload via existing `AzureContainer.PublicUserAssets` SAS flow (reuse `uploadProfileImage` pattern from user profile); displayed in the sidebar room list, room header, and member hover cards; editable in room settings behind `ManageRoom`; empty string = no image set
- [x] **Per-room nickname** — `nickname text NOT NULL DEFAULT ""` (max 32 chars) on `usersToRooms`; `updateUserToRoom({ roomId, nickname })` mutation (own nick: any member; others' nick: `ManageNicknames` permission — pending); `readNicknames({ roomId, userIds })` returns public-only fields for members; `readMyUsersToRooms({ roomIds })` returns full private row for self; `nicknameMap` in store separate from private self-data; **My Profile** room settings tab for all members; falls back to global username when empty — [ ] `ManageNicknames` permission for setting others' nicknames still pending

---

## Unread Indicators

- [x] **Unread room dot** — compare `usersToRooms.lastMessageAt` (already written by slowmode on each send) to `rooms.updatedAt` to determine "has new messages since last visit"; update `lastMessageAt` on room focus/navigation switch (currently only updated on message send); show a filled dot in the sidebar room row when `lastMessageAt < rooms.updatedAt` and room is not currently active; no new column needed — reuses existing fields; no per-message reads; no unread count (Discord shows counts, we skip to avoid read-receipt semantics already deferred twice)

---

## Mentions

- [ ] **Role mentions** — `@RoleName` in composer suggestion list (roles rendered below special mentions, above members); TipTap Mention extension already supports multiple suggestion sources — add a second `MentionType.Role` span type; server: in `getPushSubscriptionsForMessage`, detect role mention spans, query `usersToRoomRoles` for all members of that role, add to the push target set respecting their `notificationType`; permission: none (any member can @role, same as Discord default); store as `<span data-type="mention" data-mention-type="role" data-id="{roleId}" data-label="{roleName}">@{roleName}</span>` — role color applied client-side from `memberMap` store

---

## Group DM Management

- [ ] **Add / remove participants from group DMs** — `createDirectMessageParticipant({ roomId, userId })` and `deleteDirectMessageParticipant({ roomId, userId })` procedures behind `RoomType.DirectMessage` guard; `createDirectMessageParticipant` inserts into `usersToRooms` + emits system message "X was added by Y"; `deleteDirectMessageParticipant` deletes from `usersToRooms` (self-remove = leave); participant list shown in DM header chip row with an "Add People" button (picker from friends list); regenerates `participantKey` on change; blocked users cannot be added
