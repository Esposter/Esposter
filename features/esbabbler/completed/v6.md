# Esbabbler — v6 Roadmap

All features are free-to-implement (no paid infra) unless explicitly noted.
Built on existing Azure Table Storage, Postgres, Azure Blob, and Azure Function infrastructure.
Strikethroughs are acknowledged but deferred with rationale.

---

## Quick Wins

1. ~~**Migrate profile image upload off `octetInputParser`**~~ — done: `generateProfileImageUploadUrl()` in user router, `uploadBlocks` in `User/ProfileCard/Column/Image.vue`, `MAX_FILE_REQUEST_SIZE` in `formRules.ts`
2. ~~**Room profile image (SAS generate)**~~ — done: `generateProfileImageUploadUrl({ roomId })` in room router behind `ManageRoom`

---

## File Upload Architecture Fix ✓

### ~~Remove `octetInputParser` — migrate to SAS pattern~~ (done)

Replaced `uploadProfileImage` (`octetInputParser`) with `generateProfileImageUploadUrl()` in `user.ts`. Returns `{ publicUrl, sasUrl }`. Client calls `uploadBlocks(file, sasUrl)` then `updateUser({ image: publicUrl })`. `formRules.requireAtMostMaxFileSize` updated to `MAX_FILE_REQUEST_SIZE` (10 MB). Tests added in `user.test.ts`. See [`../../architecture/file-uploads.md`](../../architecture/file-uploads.md) for pattern reference.

---

## Room Identity

### ~~Room profile image (SAS procedures)~~ (done)

`image text NOT NULL DEFAULT ""` on `rooms` — already in place, no migration needed.

`generateProfileImageUploadUrl({ roomId })` added to room router behind `ManageRoom`. Returns `{ publicUrl, sasUrl }`. Tests added in `room/index.test.ts`.

- [x] **Display**: sidebar room list, room header, member hover cards. Empty string = no image (avatar initial fallback).
- [x] **Settings UI**: edit room header dialog — Discord-style clickable avatar with pencil overlay; hidden `<input type="file">`; calls `generateProfileImageUploadUrl` + `uploadBlocks` + `updateRoom({ image: publicUrl })`; `UpdateRoomInput` extended with `image`; remove icon calls `deleteProfileImage`.

---

## Group DM Management

- [x] **Create / delete participants in group DMs** — `createDirectMessageParticipant({ roomId, userId })` and `deleteDirectMessageParticipant({ roomId, userId })` procedures behind `RoomType.DirectMessage` guard; `createDirectMessageParticipant` inserts into `usersToRooms` + emits system message "X was added by Y"; `deleteDirectMessageParticipant` deletes from `usersToRooms` (self-delete = leave); participant list shown in DM header chip row with an "Add People" button (picker from friends list); regenerates `participantKey` on change; blocked users cannot be added
