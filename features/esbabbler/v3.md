# Esbabbler — v3 Roadmap

---

## Completing v2 Open Items

- [ ] **Moderator force-mute** — blocked on RBAC system; see `specs/moderation.md`

---

## RBAC System

Spec: [`specs/rbac.md`](specs/rbac.md)

- [ ] `RoomPermission` enum — `packages/shared/src/models/room/RoomPermission.ts`
- [ ] `roomRoles` Drizzle schema + migration (seed `@everyone` for existing rooms)
- [ ] `usersToRoomRoles` Drizzle schema
- [ ] RBAC service functions — `server/services/room/rbac/`
- [ ] `getOwnerProcedure` + `getPermissionsProcedure`
- [ ] Wire `createRoom` — insert @everyone + Admin roles
- [ ] Retire `getCreatorProcedure` — migrate callers
- [ ] `roleRouter` — CRUD + assign/revoke + `onRoleUpdate` subscription
- [ ] `readMembers` enrichment — include member roles
- [ ] Role assignment UI — context menu on member list
- [ ] Tests — RBAC service functions (`getPermissions`, `hasPermission`, `getTopRolePosition`, `isManageable`) + `role.test.ts` (all procedures, owner/Administrator bypass, hierarchy)

---

## Moderation System

Spec: [`specs/moderation.md`](specs/moderation.md)

- [ ] `AdminActionType` enum
- [ ] `moderationRouter` — `executeAdminAction`, `onAdminAction`, `readModerationLog`
- [ ] Force-mute / force-unmute client handler
- [ ] Kick from voice client handler
- [ ] Kick from room — server deletes `usersToRooms`
- [ ] Timeout — enforce in `createMessage`
- [ ] Ban — cascade delete + `bans` insert; `readBans`/`unbanUser`
- [ ] Moderation log — Azure Table; "Audit Log" tab in room settings
- [ ] Tests — `moderation.test.ts`; cover all action types, permission gates, subscription

---

## User Profile Editing

Spec: [`specs/user-profile.md`](specs/user-profile.md)

- [ ] `bio` column on `users` — migration
- [ ] `updateUser` procedure
- [ ] Avatar upload — `/avatars/{userId}` blob path
- [ ] Edit profile dialog — bottom-left user panel
- [ ] Tests — `user.test.ts`; cover `updateUser` validation, auth guard, field limits

---

## Out of Scope

- ~~**Video / screen share**~~ _(Requires SFU — deferred until LiveKit provisioned.)_
- ~~**Custom emoji per room**~~ _(Azure Blob cost + moderation complexity; low ROI.)_
- ~~**Per-channel permission overrides**~~ _(Rooms are flat; revisit if sub-channels added.)_
