# `useDrag()`

A composable for drag interactions.

## Why not

- `useInitializeGameObjectEvents` already exposes game object events including drag; a separate composable would be a thin wrapper over existing functionality.
