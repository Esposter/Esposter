# Scene Transition Effects

Built-in transition variants for switching scenes.

## Why not

- App-specific; the caller should own transition logic. The library's `switchToScene()` provides the hook; transition variants belong in the consuming app.
