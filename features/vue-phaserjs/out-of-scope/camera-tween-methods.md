# Camera Tween Helpers (`flashCamera` / `shakeCamera` / `panCamera` / `zoomCamera` / `rotateTo`)

Wrapper helpers for the camera flash/shake/pan/zoom/rotate effects.

## Why not

- Not currently used in the codebase. Add a wrapper only when a real call site needs one; until then, call the Phaser camera API directly.
