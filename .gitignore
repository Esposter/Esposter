# Dependencies
node_modules

# Pnpm artifacts
.pnpm-store

# virrun local cache (repo-local store + warm snapshots; disposable, repopulated on next routed run)
.virrun

# Build Artifacts
**/src/index.ts
**/src/components/index.ts
**/public/docs

# Nuxt
.output
.data
.nuxt
.nitro
.cache
dist
nuxt-build.cpuprofile

# Typescript build info
*.tsbuildinfo

# Testing
coverage
.vitest-reports
vitest.config.*.timestamp*

# Local env files
.env
.env.*
!.env.example

# Local db
*.db
*.db-journal

# Logs
logs
*.log

# Tiled
*.tiled-session

# VSCode local workspace
*.code-workspace

# Agent tree (`.claude` is a symlink alias to `.agents`, so it needs no entry of its own)
scheduled_tasks.lock
.agents/worktrees

# OS
.DS_Store
