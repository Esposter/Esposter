// The dock shows every bookmark in full, so a reader keeps a handful rather than a list the dock would have to scroll
export const MAX_BOOKMARKS = 10;
export const BOOKMARK_PATH_MAX_LENGTH = 2048;
export const BOOKMARK_TITLE_MAX_LENGTH = 100;
