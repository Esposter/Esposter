// The working copy is rewritten on every autosave, so its level buys speed rather than the last few percent of
// Ratio — on a Sheet's rows the fastest level also compresses best, where the version store's level spends
// Several times as long for less (writeResourceContentBlob.bench.md)
export const CONTENT_COMPRESSION_LEVEL = 1;
// RFC 9659: an encoder behind `Content-Encoding: zstd` must not need a larger window, since a browser's decoder is
// Only required to support this much — a stored format rather than a tunable
export const MAX_CONTENT_ENCODING_WINDOW_LOG = 23;
