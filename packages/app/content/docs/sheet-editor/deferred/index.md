---
title: Deferred
description: Ideas the sheet editor chose not to build yet — each with the concrete trigger that would revisit it.
---

# Deferred

Ideas we chose not to build yet. One page per idea; each states why and the concrete trigger that would revisit it. Check here (and [rejected](/docs/sheet-editor/rejected)) before proposing an idea.

- [Computed-value cache](/docs/sheet-editor/deferred/computed-value-cache) — memoize computed-column evaluation instead of recomputing on every read.
