---
title: Markdown export
description: Expose the internal markdown serializer as an official export format.
---

# Markdown export

Exposing the existing `serializeToMarkdown.ts` as an official export format.

**Why not:** Markdown is not a standard data-interchange format, and copy-to-clipboard already covers the shareability use case.
