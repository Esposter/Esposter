// One indentation step per level below the comment the route names, and the level the stepping stops at — the
// Nesting itself stays unbounded
export const COMMENT_INDENT_STEP = "1.5rem";
export const MAX_COMMENT_INDENT_DEPTH = 6;
