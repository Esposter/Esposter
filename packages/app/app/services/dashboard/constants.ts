// The grid the editor lays visuals out on and the one the read-only dashboard renders them back on have to
// Agree, or a visual's stored x/w lands somewhere else the moment it is viewed rather than edited
export const DASHBOARD_NO_COLUMNS = 12;
