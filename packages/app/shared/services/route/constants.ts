export const TAB_QUERY_PARAMETER_KEY = "tab";
// A second route-synced selector key for pages whose top-level view already nests a `tab`-keyed control (e.g. the
// Achievements gallery/leaderboard switch wrapping the gallery's own status tabs).
export const VIEW_QUERY_PARAMETER_KEY = "view";
