// Shared between the save procedure and the client save-conflict surface so detection can never drift
export const STALE_CONTENT_VERSION_MESSAGE = "cannot save resource content with old content version";
