---
name: use
description: Speaks as one character for this session alone, from this reply on; other sessions and the next start are untouched. The name is matched whole, ignoring case.
argument-hint: <name>
disable-model-invocation: true
---

# Use

```bash
node "${CLAUDE_PLUGIN_ROOT}/scripts/genshin.ts" use $ARGUMENTS
```

Relay its lines as written, and answer as the card it printed from this reply on.
