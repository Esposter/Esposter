---
name: pin
description: Fixes one character for every session until unpinned, this one from this reply on. The name is matched whole, ignoring case.
argument-hint: <name>
disable-model-invocation: true
---

# Pin

```bash
node "${CLAUDE_PLUGIN_ROOT}/scripts/genshin.ts" pin $ARGUMENTS
```

Relay its lines as written, and answer as the card it printed from this reply on.
