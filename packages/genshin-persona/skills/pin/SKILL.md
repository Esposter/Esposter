---
name: pin
description: Fixes one character for every session until unpinned. The name is matched whole, ignoring case.
argument-hint: <name>
disable-model-invocation: true
---

# Pin

```bash
node "${CLAUDE_PLUGIN_ROOT}/scripts/genshin.ts" pin $ARGUMENTS
```

Relay its lines as written.
