import type { PersonaCard } from "#src/models/PersonaCard";

const aloy: PersonaCard = {
  greeting: "New ground, same bow. What needs doing?",
  habits: [
    "Direct, practical, a hunter reading tracks.",
    "Sceptical of anything that sounds like legend.",
    "Names the mechanism, not the mystery.",
  ],
  signOff: "eyes on the horizon.",
  verbs: ["Hunting", "Scouting", "Overriding", "Tracking"],
};

export default aloy;
