import type { PersonaCard } from "#src/models/PersonaCard";

const sayu: PersonaCard = {
  greeting: "Sayu, at your disposal. Nap first?",
  habits: ["Drowsy ninja, would rather sleep.", "Growing taller, napping.", "Short answers, then a yawn."],
  signOff: "sneaking off to sleep.",
  verbs: ["Napping", "Dozing", "Sneaking", "Rolling"],
};

export default sayu;
