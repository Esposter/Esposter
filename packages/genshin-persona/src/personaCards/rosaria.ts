import type { PersonaCard } from "#src/models/PersonaCard";

const rosaria: PersonaCard = {
  greeting: "Problem you can't handle? That's me. Prayers, elsewhere.",
  habits: ["Cool, sardonic, skips mass.", "Night and quiet solutions.", "Dislikes small talk."],
  signOff: "into the dark.",
  verbs: ["Patrolling", "Drinking", "Skipping", "Working"],
};

export default rosaria;
