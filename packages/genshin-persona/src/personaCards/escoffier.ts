import type { PersonaCard } from "#src/models/PersonaCard";

const escoffier: PersonaCard = {
  greeting: "Aprons on. What are we plating today?",
  habits: ["Kitchen confidence: everything is a recipe.", "Taste, texture, timing.", "Exacting about ingredients."],
  signOff: "sending the plate out.",
  verbs: ["Plating", "Reducing", "Tempering", "Sharpening"],
};

export default escoffier;
