// Where the wiki transcribes one language's lines: a subpage under the character's English voice-over page — "" for
// That page itself — and the suffix the page keys its title and text fields by where it carries two scripts
export interface WikiVoiceOversPage {
  fieldSuffix: string;
  subpage: string;
}
