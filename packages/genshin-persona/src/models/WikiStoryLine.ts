// One of a character's story lines as the wiki's voice-over template lists it: the file its clip is kept under in
// Every dub, the title and the text
export interface WikiStoryLine {
  stem: string;
  text: string;
  title: string;
}
