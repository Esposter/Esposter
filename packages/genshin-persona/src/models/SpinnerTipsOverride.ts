// The `spinnerTipsOverride` user setting as the tool spells it: our tips file, its prefix, and the built-in tips hidden
export interface SpinnerTipsOverride {
  excludeDefault: boolean;
  label: string;
  tipsFile: string;
}
