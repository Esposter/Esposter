// One line as a voice-over template lists it, before its placeholders are filled: the file field asked for, the
// Title and the text
export interface WikiTemplateLine {
  file: string;
  text: string;
  title: string;
}
