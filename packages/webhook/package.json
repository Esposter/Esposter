{
  "name": "@esposter/webhook",
  "description": "A library that contains webhook functionality.",
  "version": "2.14.0",
  "private": true,
  "type": "module",
  "homepage": "https://github.com/Esposter/Esposter/blob/main/packages/webhook#readme",
  "license": "Apache-2.0",
  "author": "Jimmy Chen",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    ".funcignore"
  ],
  "keywords": [
    "webhook"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Esposter/Esposter.git"
  },
  "bugs": {
    "url": "https://github.com/Esposter/Esposter/issues"
  },
  "scripts": {
    "build": "pnpm export:gen && rolldown --config rolldown.config.ts",
    "export:gen": "ctix build --config ../configuration/.ctirc-ts",
    "lint": "oxlint && eslint .",
    "lint:fix": "oxlint --fix && eslint --fix .",
    "typecheck": "tsc"
  },
  "dependencies": {
    "@azure/functions": "catalog:",
    "@esposter/shared": "workspace:*",
    "zod": "catalog:"
  }
}
