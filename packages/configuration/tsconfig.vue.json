{
  "extends": "../app/.nuxt/tsconfig.json",
  "compilerOptions": {
    "baseUrl": "${configDir}/src",
    "paths": {
      "@/*": ["*"]
    }
  },
  "include": ["types/*.d.ts", "${configDir}/*.ts", "${configDir}/src/**/*.ts", "${configDir}/src/**/*.vue"],
  "exclude": ["${configDir}/node_modules"]
}
