// What both halves of the directive-reason check report, beside the pattern they read in `directiveReasonRegex.js`.
export default "A disable directive carries its reason after ` -- ` on the directive itself, so the next reader can tell a load-bearing exception from a stale one. See the oxlint skill.";
