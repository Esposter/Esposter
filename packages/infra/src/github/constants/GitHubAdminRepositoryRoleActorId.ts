// GitHub's standard built-in repository role IDs are fixed across all repositories;
// 5 is the Admin role. Used as a ruleset `bypassActors` actorId with actorType
// "RepositoryRole".
const GitHubAdminRepositoryRoleActorId = 5;

export default GitHubAdminRepositoryRoleActorId;
