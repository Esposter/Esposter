{
  "name": "@esposter/infra",
  "version": "2.24.0",
  "private": true,
  "description": "Pulumi infrastructure code and migration tools for Esposter Azure resources.",
  "keywords": [
    "azure",
    "iac",
    "infrastructure",
    "pulumi",
    "typescript"
  ],
  "homepage": "https://github.com/Esposter/Esposter/blob/main/packages/infra#readme",
  "bugs": {
    "url": "https://github.com/Esposter/Esposter/issues"
  },
  "license": "Apache-2.0",
  "author": "Jimmy Chen",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Esposter/Esposter.git"
  },
  "type": "module",
  "scripts": {
    "build": "pnpm export:gen && pnpm typecheck",
    "export:gen": "ctix build --config ../configuration/.ctirc-ts && tsx src/scripts/fixGeneratedIndexExtensions.ts",
    "format": "oxfmt",
    "format:check": "oxfmt --check",
    "import": "pnpm export:gen && pulumi import --file generated/azure-import-manifest.json",
    "inventory:build": "tsx src/scripts/buildAzureImportManifest.ts",
    "lint": "oxlint && eslint .",
    "lint:fix": "oxlint --fix && eslint --fix .",
    "preview": "pnpm export:gen && pulumi preview",
    "refresh": "pnpm export:gen && pulumi refresh",
    "typecheck": "tsgo",
    "up": "pnpm export:gen && pulumi up"
  },
  "dependencies": {
    "@pulumi/azure-native": "catalog:",
    "@pulumi/pulumi": "catalog:"
  },
  "devDependencies": {
    "@esposter/configuration": "workspace:*"
  }
}
