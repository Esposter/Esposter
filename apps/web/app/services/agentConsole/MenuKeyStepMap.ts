// A menu over the world walks with W and S as a game's does, as well as with the arrows: each key and the way it moves
export const MenuKeyStepMap: Record<string, number> = { arrowdown: 1, arrowup: -1, s: 1, w: -1 };
