export const EMOJI_PICKER_TOOLTIP_TEXT = "Add Reaction";
// Every trigger the picker opens from ends a row near the foot of what it acts on — the composer's toolbar, a
// Message's hover bar, its reactions — so the panel opens above it and grows toward the row's start, and flips below
// Only where the top has no room
export const EMOJI_PICKER_POSITION_AREA = "top span-left";
