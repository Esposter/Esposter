// The boolean filter's choice that filters nothing, keyed by a value of its own since a select keys every choice
export const ALL_BOOLEAN_FILTER_VALUE = "all";
export const DRAG_HANDLE_CLASS = "drag-handle";
// The page sizes the grid offers, -1 for every row
export const SHEET_ITEMS_PER_PAGE_OPTIONS = [10, 25, 50, 100, -1];
export const MAX_HISTORY_SIZE = 50;
export const OUTLIER_HIGHLIGHT_CLASS = "bg-orange-100 ring-1 ring-orange-400";
export const OUTLIER_STANDARD_DEVIATION_MULTIPLIER = 2;
