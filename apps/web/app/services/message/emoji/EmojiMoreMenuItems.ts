import { EmojiMenuItems } from "@/services/message/emoji/EmojiMenuItems";

// The row the hover bar shows, plus the one more the overflow menu has room for
export const EmojiMoreMenuItems = [...EmojiMenuItems, "👌"];
