// The CSS variables one carousel card animates between: where it is and how tall it is, and where it was.
export interface CardStyleVariables {
  marginRight?: string;
  oldMarginRight?: string;
  oldScaleY?: string;
  scaleY?: string;
}
