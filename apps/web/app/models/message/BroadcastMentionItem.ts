// No avatar of its own — the suggestion list renders an icon for a broadcast, and the `image in item`
// Branch it shares with a member row is what tells the two apart
export interface BroadcastMentionItem {
  id: string;
  name: string;
}
