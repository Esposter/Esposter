// A button's look beyond the raised default: the accent for the one action a panel leads with, danger for one that
// Destroys or stops something, and quiet for a mark that floats over content without a surface of its own
export enum UiButtonVariant {
  Accent = "Accent",
  Danger = "Danger",
  Quiet = "Quiet",
}
