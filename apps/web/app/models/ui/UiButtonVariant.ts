// A button's look beyond the raised default: the accent for the one action a panel leads with, danger for one that
// Destroys or stops something, quiet for a mark that floats over content without a surface of its own, and search for
// The trigger drawn as the search field it opens
export enum UiButtonVariant {
  Accent = "Accent",
  Danger = "Danger",
  Quiet = "Quiet",
  Search = "Search",
}
