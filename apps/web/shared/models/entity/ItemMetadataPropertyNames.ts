import type { ItemMetadata, PropertyNames } from "@esposter/shared";

import { getPropertyNames } from "@esposter/shared";

export const ItemMetadataPropertyNames: PropertyNames<ItemMetadata> = getPropertyNames<ItemMetadata>();
