// The number of top-ranked users the points leaderboard returns; the caller's own row is appended when it falls
// Outside this window (readPointsLeaderboard).
export const MAX_POINTS_LEADERBOARD_ENTRIES = 25;
// What readAchievementMap shows for a hidden achievement's description until the caller unlocks it
export const HIDDEN_ACHIEVEMENT_DESCRIPTION = "???";
// The emojis one message carries to earn EmojiLover
export const EMOJI_LOVER_EMOJI_COUNT = 10;
