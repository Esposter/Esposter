// Renovate (Mend) is a global GitHub App with the fixed app id 2740, identical across
// Every installation. Used as a ruleset `bypassActors` actorId with actorType
// "Integration" so Renovate can keep opening its dependency-update branches/PRs.
const GitHubRenovateAppActorId = 2740;

export default GitHubRenovateAppActorId;
