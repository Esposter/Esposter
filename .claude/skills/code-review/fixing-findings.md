# Closing a finding so the next review cannot reopen it

Read this when **applying** fixes, not when running a review. It is a pre-commit checklist for whoever writes the fix, and the block to paste into the prompt whenever a fix round is delegated (`model-delegation` skill).

A re-run over an already-fixed branch should surface only genuinely new defects. The workflow labels each finding's `provenance` for exactly this: anything other than `new` means the review has been here before, and the label names which of the failures below it is. When it surfaces the same area again, it is almost always one of them — each with a fixed remedy.

Every entry below describes a fix that shipped, passed its own new test, and was found again one round later.

- **The fix was applied to N−1 of N sites, and converge is the remedy, not N tests.** A fix that introduces a flag, a helper, an ordering, or any other invariant is not done until every existing site that could hold it does. Find them by grepping the thing the fix touched — the new parameter, the helper's name, the field being written — because the site nobody thought about is exactly the one that stays broken (a `isPublishedAssetCloned` flag threaded through the duplicate path while the restore path beside it kept the old call; a room-keyed write helper adopted by two of its three callers). **The first move is to collapse those sites onto one primitive that cannot be half-applied**, not to copy the fix and its test into each of them: the duplication is why the invariant could drift, so a per-site test only pins the drift in place and multiplies the tests a later refactor has to rewrite. Once one function owns the behaviour, one test covers it and the call sites need none — what remains to test is wiring (does this caller call it, with this room's id) and genuine business logic. Extract per the `file-organization` skill; collapse the tests per the `testing` skill's behaviour-matrix rule. Only where the sites genuinely cannot converge does each need its own fix and test.
- **The fix's claim was never pinned by a test — including a fix to the review workflow itself.** `.claude/workflows/code-review/` drives the shipped script with stubbed agents, so a finding about parsing, scoping, dedupe, confidence, resolution or report assembly has somewhere to become a test that fails against the pre-fix code. Three rounds of fixes landed there before the suite existed and two of them were re-found by the next round; the first run of the suite caught a third that a commit message had already claimed as fixed. Adding the test is part of the fix, not follow-up.
- **The fix's claim was never pinned by a test.** A commit message that says "re-mint cached urls once the SAS expires" while the code only added an expiry check to a filter that nothing re-runs is a claim, not a behaviour. Every fix lands with a test that fails against the pre-fix code, or it is not done.
- **The fix bought its guarantee by exempting one value from an existing guard.** "Force this past the age filter / the dedupe / the retry cap" closes the finding in front of you and opens the next one, because that guard was the only thing standing between the operation and a value it must never touch — and the value a fix wants to exempt is characteristically the one another actor just wrote. So before adding an exemption, **name the guard and what it was protecting**; where that protection is real the finding is closed by deferring the work to the next pass, never by escaping the guard, and the deferral is what gets written down. An exemption that genuinely must ship lands with the test that **crosses** it — the guard's own case, plus the exempted value under exactly the condition the guard exists for. This is the single most common way a fix round introduces a worse defect than the one it closed.
- **A comment asserted a mitigation that does not exist.** "A stray entry here is swept with the rest by `cache clean`" reads to every later reviewer, and to every finder, as a closed loop — so the gap it hides survives review after review, and the comment is what stops anyone looking. A comment that names a mechanism is a claim about code: either that mechanism exists and a test pins it, or the comment states plainly that nothing reclaims it. Never assert a cleanup path you have not opened.
- **The record still describes the old behaviour.** Docs and skills are the tiebreaker a verifier greps, so a stale line is worse than no line: it argues _for_ reopening. When a fix changes behaviour the docs describe, the doc edit is part of that fix, not follow-up.
- **The fix picked a side of a guess the code cannot make.** The code is deciding something it has no recorded fact for — is this thumbnail blob missing or is its read SAS expired, is this cache entry abandoned or mid-run, is this node version real or a degraded probe, is this blob name free or does a pending delete still target it — and the fix flips which side of the inference it trusts. Both sides are wrong for the other case, so the next review confirms the opposite finding on the same lines, forever, at whatever period the two failure modes alternate. **Name the missing fact and record it at the moment it is known** (write it on the entity, mint a name that cannot collide, publish the claim before the sweep decides, fail instead of substituting a probe) — then the decision reads the fact instead of guessing, and the finding cannot come back in either direction. A fix that reads "flip the flag / invert the default / trust the other signal" is this entry until proven otherwise.
- **The fix added a guard, and an existing test's mock walks straight into it.** A guard that rejects an empty/degraded input is right, and the suites that stubbed that input with exactly the empty value are now asserting the guard instead of their own subject — so they fail, or worse pass for the wrong reason, and only on the platform the guard checks (`process.platform === "win32"` fails locally while CI's Linux short-circuits it, so nothing catches it). Adding a guard means grepping for every mock of the value it guards. The remedy is one shared fixture for that stub — a single exported constant the suites mock with — so the guard's own case is the one place that sets the rejected value, in place. N hand-rolled copies is how the drift started.
- **The fix compensated for another fix instead of removing it.** A finding says a mechanism strands data; the fix adds a second mechanism that goes and finds the stranded data — a sweep, a reconciler, a timer that re-reads what a push was supposed to deliver. It closes the finding and permanently adds a polling loop that costs something on every run forever, to cover a window that only exists because of the first mechanism. **Ask what opens the window before building anything that closes it**: here the window was a budget guard tearing down the one subscription that made dead letters recoverable, and not tearing it down deleted the window, the poller, and the role assignments the poller's cousin needed. Prefer deleting the cause over adding a compensator; a compensator is right only when the cause is genuinely load-bearing, and then it says so in the same commit.
- **The claim was checked against the vendor's documentation instead of the deployed resource.** An infra finding reasons "identity-based X requires companion setting Y; Y is absent, so this breaks" — sound from the docs, and wrong about a system that has been running for months. One read-only query (`az functionapp config appsettings list --query "[].name"`) settles it and costs nothing. Never change a running deployment on a reading of the provider docs alone, and when the live state is the answer, **write the verified state into the record** with the command that produced it — otherwise the same doc-derived argument arrives again next review.
- **The fix's new guard has a fallback that fires exactly when the guard matters.** A cap, a filter, or a resolution step lands with an "if this resolves to nothing, use the raw input" escape — and the case that resolves to nothing is the case the guard was built for (the seam whose files the cap just dropped, the lookup whose key the new validation just rejected). So the bound holds on every run that did not need it and is bypassed on the one that did, while the log still reports the bounded number. **A fallback must be at least as loud as the guard**: name the empty case as out-of-scope, or fail, but never route it back to the unbounded input silently. Before shipping one, ask which run reaches it — if the answer is "the one the guard exists for", it is not a fallback, it is a hole.
- **The fix added a threshold, and a failure produces a value that satisfies it.** A quality gate lands — a confidence floor, a freshness cut-off, a minimum score — and it filters on a field that is _also_ what a crashed, timed-out, or skipped step leaves behind. So the gate does its job on healthy runs and silently deletes the evidence on exactly the degraded ones, where the missing rows read as "nothing was found" rather than "nothing could be judged". **A threshold must distinguish a low answer from no answer**: record whether the step that produces the value actually ran, gate on that, and let the no-answer case through carrying its reason. The same applies to aggregating a number across members that disagree — take it from the ones that reached the conclusion being reported, never the maximum over all of them.
- **The fix keyed a merge on fewer fields than the thing it merges actually varies by.** A dedupe, cache, or group-by added to stop duplicate output keys on the obvious identity (a location, an id) while the values differ along a dimension the record keeps separate — a code fix and the doc edit describing it, two kinds of error at one line. The collision then discards a whole deliverable, and the surviving row is stamped with the loser's label or a corroboration count that means the opposite of what it says. **The key is every field a downstream consumer distinguishes**, and the written record is the list: where a page says two things are separate deliverables, that distinction belongs in the key.
- **The decision was deliberate but written nowhere.** A dropped lint rule, an accepted cost, a revocation that must stay uncached — undocumented, these read as defects on every run. Write the decision **with its consequence named** ("nothing enforces this; an unterminated chain fails silently"), which is what stops the argument rather than inviting it. An `area` review reports these directly as `record-gap` findings, which is the cheapest time to close them — before they have drawn fire on three separate diff reviews.

The dominant defect class on a re-review is not a missed bug — it is a **regression introduced by the previous round's fixes**, and specifically at the seam where two independently-tested features meet (guards run before a claim × a guard that now has side effects; roll back on failure × a step that runs after the commit succeeds). Each feature's own tests stay green because each is right in isolation, and a round that adds one test per fix still leaves the seam untested — successive rounds can each land a passing test on the same few lines while the defect between them survives every one.

So the crossing test is a **requirement of any fix that edits a line an earlier fix wrote**, not an option: `git log -L <range>:<file>` on the lines you are about to change names the features to cross, and the diff of the last round's fix commits is the cheapest place to look for the next seam.

**This list is append-only, and extending it is part of applying a fix.** When a finding arrives with `provenance: regression` or `reopened` and none of the entries above names why it came back, the round is not done until the new cause is written here as its own entry — same shape: what the fix did, why the next review re-finds it, and the remedy that ends it rather than alternating it. The list is the only thing that converges; a cause that stays unwritten is re-derived, half-remembered, and re-shipped every round.

## Order of work

Land the substantive work in this order: **root-cause fix → converge the call sites onto the primitive → docs and skills → then the check pass.** Batching the checks and diffing against a clean tree first are the `context-efficiency` skill's rules; this order is what a fix round adds to them.

The order matters because the trivial errors crowd out the architectural decision still to be made: a session that spends its remaining budget on a fixture's missing field ships the symptom fix and queues the root cause.

One corollary specific to fixing findings: **deleting the compensating code is part of the fix, not follow-up.** When a root-cause fix makes a wrapper, guard, or flag unnecessary, it comes out in the same change. A wrapper left behind is a wrapper someone must remember to apply, which is the defect class the fix just removed.
