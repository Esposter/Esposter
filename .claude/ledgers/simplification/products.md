# Products

The app's smaller products — everything under `app/components` that is neither messaging nor the resource explorer. Each row carries a product's components together with the store, composable, service and page files only it uses.

| Unit                                                                                                                  | Swept      | Notes                                                                                                                                                                                                         |
| --------------------------------------------------------------------------------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `Post` + `store/post`, `composables/post`, `services/post`, `pages/post`                                              | 2026-08-14 | `useReadPosts` absorbed its user-filtered twin; `PostVoteButton` owns the vote cascade both directions had written out, pinned by a test; three re-emit shims inlined                                         |
| `Clicker` + `store/clicker`, `composables/clicker`, `services/clicker`, `pages/clicker.vue`                           | 2026-08-15 | `shared/models/clicker` swept 2026-08-12 — shared models not in scope; the two timer composables became `useWorkerInterval`, pinned by an unmount test; icon globs hoisted to module-scope maps               |
| `User`, `Achievement` + `store/achievement`, the `user`/`achievement`/`room` layers, `pages/user`, `achievements.vue` | 2026-08-15 | `useReadUserAchievements` absorbed the snackbar list's copy of the read; one `getUnlockedUserAchievements` behind three filters; `CategoryColorMap` replaced the switch; dead `isAchievementUnlocked` deleted |
| `Docs` + `composables/docs`, `services/docs`, `pages/docs`, `pages/[...slug].vue`                                     | 2026-08-15 | thin pass — one `getSlug` behind four copies of the last-path-segment idiom, and the toc listeners stopped taking bare function references                                                                    |
| `RichTextEditor` + `composables/codemirror`, `services/codemirror`                                                    | —          |                                                                                                                                                                                                               |
| `Visual`, `Anime`, `About`, `Login`, `Transition` + their composable/service layers and the pages that mount them     | —          |                                                                                                                                                                                                               |
