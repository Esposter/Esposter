# Shared

| Unit                                                       | Swept      | Notes                                                                                                                                                                                                                                                                                              |
| ---------------------------------------------------------- | ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| optimistic rollbacks                                       | 2026-08-09 | `applyOptimistic` sites + hand-rolled ones; two bugs → `pinia` skill                                                                                                                                                                                                                               |
| `app/components/Styled`                                    | 2026-08-11 | `StyledNavList` extracted; one app-level clipboard snackbar replaces the per-row ones; `StyledDialog` grew an action-less form and a pinned `#header`, both pinned by its own new test; both `StyledDataTable` wrappers deleted into `globals.scss`; effect-scope rule → `vue-composable-patterns` |
| `shared/models/db` + `shared/models/resource`              | 2026-08-10 | Two over-permissive schemas tightened and pinned; the composite key and the normalized-string helpers each own one home                                                                                                                                                                            |
| `shared/models/dungeons`                                   | 2026-08-12 | `Settings` was a Record over one union of every option's value type; the exhaustiveness moved to a `satisfies` so each key keeps its own. The two `Keys` shapes — Set for `has`, array for iteration — each match their callers and stay                                                           |
| `shared/models/clicker`                                    | 2026-08-12 | Second and last `id: z.uuid()` restated over the `aItemEntitySchema` spread. `Clicker` and `ClickerSave` stay two classes — resolved state and persisted state, not near-twins to collapse                                                                                                         |
| `shared/models/achievement` + `services/achievement`       | 2026-08-12 | `category` was restated on all 85 definitions when the file it sits in already fixes it — `defineAchievementDefinitionMap` supplies it once per map, pinned by a test since the injection runs through a cast. Four condition types onto `ItemEntityType`                                          |
| `shared/services/resource`                                 | 2026-08-12 | Nothing to collapse. `ResourceDefinitionMap.title` restates the type nine times out of ten, and defaulting it would make eight call sites state more than they do now. `ColumnValue`'s `null` is the domain's empty cell → `typescript` absent-values                                              |
| `shared/models/message` + `services/message`               | 2026-08-12 | Already single-source: the operation-permission map, `getIsMessageAuthor` and the permission resolver each answer one question once. One `""` comparison carried off a truthiness check; the dedupe test says what it pins                                                                         |
| `shared/models/dashboard` + `dataset` + `services/dataset` | 2026-08-12 | Nothing to change: the visual, chart and query models each carry one concern and the `ChartType` annotation that looks redundant is load-bearing over a merged union                                                                                                                               |
| `shared/models/flowchartEditor`                            | 2026-08-12 | A faithful mirror of @vue-flow/core, so the nulls stay and now say why. `generalNodeTypeSchema` was the one enum schema without its `satisfies` → `zod` skill                                                                                                                                      |
| `emailEditor` + `webpageEditor` + `grapesjs`               | 2026-08-12 | Found the `.catchall` both subclass schemas lost when `.extend()` became a `.shape` spread in 91e380bdb — GrapesJS state was being stripped on parse. Fixed, pinned, → `zod` skill                                                                                                                 |
| `shared/models/pagination` + `services/pagination`         | 2026-08-12 | `OffsetPaginationData` re-ran the base constructor's `Object.assign`; the empty subclass stays, it names the offset half of the pair                                                                                                                                                               |
| `shared/models/compiler` + `services/compiler`             | 2026-08-12 | `createVariableRegex` hand-rolled regex escaping twice where `RegExp.escape` is already the repo's choice                                                                                                                                                                                          |
| the small shared roots                                     | 2026-08-12 | The relative imports in `ImageSourceWhitelist` are load-bearing — nuxt.config evaluates it before aliases exist — and now say so; six repeated `// Grapesjs` comments became one                                                                                                                   |
| `packages/shared` — `src/models`, `src/test`               | 2026-08-12 | The five error classes keep their literal `this.name` — a `new.target.name` base is the obvious dedupe and the minifier breaks it → `error-handling`. `RoutePath`'s restated type is isolatedDeclarations, not duplication                                                                         |
| `packages/shared` — `src/services`                         | —          |                                                                                                                                                                                                                                                                                                    |
| `packages/shared` — `src/util/types`                       | —          |                                                                                                                                                                                                                                                                                                    |
| `packages/shared` — the rest of `src/util`                 | —          | `array`, `environment`, `function`, `id`, `map`, `object`, `promise`, `reactivity`, `text`, `time`, `validation`, `zod`                                                                                                                                                                            |
| `packages/shared-node`                                     | —          |                                                                                                                                                                                                                                                                                                    |
