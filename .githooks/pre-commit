#!/bin/sh
set -e
STAGED=$(git diff --cached --name-only --diff-filter=ACMR)
[ -z "$STAGED" ] && exit 0
git diff --cached --name-only --diff-filter=ACMR -z | xargs -0 -L 50 ./node_modules/.bin/oxfmt
git diff --cached --name-only --diff-filter=ACMR -z | xargs -0 git add
